import javax.swing.*;
import java.awt.*;

public class HelpPanel extends JPanel {

    public HelpPanel(ParentPanel parentPanel) {
        setPreferredSize(new Dimension(1000, 1000));
        setBackground(Color.BLACK);
    }
}
